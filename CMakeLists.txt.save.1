cmake_minimum_required(VERSION 3.16)
project(vigil_node)
set(CMAKE_CXX_STANDARD 17)
include_directories(cpp/include)
add_executable(vigil cpp/src/main.cpp)
add_executable(vigil cpp/src/main.cpp)
add_executable(vigil 
    cpp/src/main.cpp 
    cpp/src/orderbook.cpp
)
cmake_minimum_required(VERSION 3.10)
project(VigilNode LANGUAGES CXX)

# Set C++ standard to C++17 or higher for structured binding support [auto [price, order]]
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# 1. Define the executable and all its source files
add_executable(vigil 
    cpp/src/main.cpp 
    cpp/src/orderbook.cpp
)

# 2. Tell CMake where to look for header files (.h files)
target_include_directories(vigil PRIVATE cpp/include)
cd build
cmake ..
make
./vigil
 
cd build
cmake ..
make
./vigil
^X

^T


































